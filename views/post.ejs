<%- include('partials/_header') -%>


  <body>
    <%- include('partials/_navigation') -%>


    <!-- Page Header -->
    <header class="masthead" style="background-image: url('img/post-bg.jpg')">
      <div class="overlay"></div>
      <div class="container">
        <div class="row">
          <div class="col-lg-8 col-md-10 mx-auto">
            <div class="post-heading">
              <h1><%= post.title %></h1>
              <h2 class="subheading">
                <%= post.message %>
              </h2>
              <span class="meta"
                >Posted by
                <a href="#"><%= post.name %></a>
                <%
        const options = { month: 'long', day: 'numeric', year: 'numeric' };
        const formatter = new Intl.DateTimeFormat('en-US', options);
        const dateString = formatter.format(post.dateCreated);
      %>
      <%= dateString %>
    </p></span
              >
            </div>
          </div>
        </div>
      </div>
    </header>

    
            </p>
          </div>
        </div>
      </div>
    </article>
    <div class="col-xl-4 col-lg-5">
                <!-- Share box -->
                <div class="tm-bg-gray tm-share-box">
                  <div class="text-center">
                    <a href="/post/edit/<%=post._id %>"
                      class="btn btn-primary p-0 mb-4 tm-btn-animate tm-btn-download tm-icon-download"
                    >
                      <span>Update Details</span>
                  </a>
                  </div>
                  <div class="text-center">
                    <button
                      class="btn btn-danger p-0 tm-btn-animate tm-btn-download tm-icon-download"
                    >
                      <span>Delete Photo</span>
                    </button>
                  </div>
                </div>
              </div>


  <%- include('partials/_footer') -%>
   

    <!-- Custom scripts for this template -->
    <script src="js/clean-blog.min.js"></script>
  </body>
</html>
